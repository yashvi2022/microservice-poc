apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task-ns
spec:
  selector:
    app: task-service
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
